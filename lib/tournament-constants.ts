/** Nombre minimum de joueurs pour démarrer un tournoi (règle métier). */
export const MIN_PLAYERS = 4;

/** Nombre de tours par défaut (règle métier, configurable en M8). */
export const DEFAULT_ROUNDS = 4;
